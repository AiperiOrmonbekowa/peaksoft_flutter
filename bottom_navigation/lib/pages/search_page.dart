import 'package:flutter/material.dart';

class SearcPage extends StatelessWidget {
  const SearcPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Page'),),
    );
  }
}